import 'package:flutter/material.dart';

class ClaudiaPage extends StatelessWidget {
  const ClaudiaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
